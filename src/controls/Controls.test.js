import {
  Button,
  FullscreenControl,
  GeolocationControl,
  ListBox,
  ListBoxItem,
  RouteButton,
  RouteEditor,
  RoutePanel,
  RulerControl,
  SearchControl,
  TrafficControl,
  TypeSelector,
  ZoomControl,
} from './Controls';

describe('Button', () => {
  it('should be a Button', () => {
    expect(Button.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(Button)))"`
    );
  });
});

describe('FullscreenControl', () => {
  it('should be a FullscreenControl', () => {
    expect(FullscreenControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(FullscreenControl)))"`
    );
  });
});

describe('GeolocationControl', () => {
  it('should be a GeolocationControl', () => {
    expect(GeolocationControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(GeolocationControl)))"`
    );
  });
});

describe('ListBox', () => {
  it('should be a ListBox', () => {
    expect(ListBox.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(ListBox)))"`
    );
  });
});

describe('ListBoxItem', () => {
  it('should be a ListBoxItem', () => {
    expect(ListBoxItem.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(ListBoxItem)))"`
    );
  });
});

describe('RouteButton', () => {
  it('should be a RouteButton', () => {
    expect(RouteButton.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(RouteButton)))"`
    );
  });
});

describe('RouteEditor', () => {
  it('should be a RouteEditor', () => {
    expect(RouteEditor.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(RouteEditor)))"`
    );
  });
});

describe('RoutePanel', () => {
  it('should be a RoutePanel', () => {
    expect(RoutePanel.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(RoutePanel)))"`
    );
  });
});

describe('RulerControl', () => {
  it('should be a RulerControl', () => {
    expect(RulerControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(RulerControl)))"`
    );
  });
});

describe('SearchControl', () => {
  it('should be a SearchControl', () => {
    expect(SearchControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(SearchControl)))"`
    );
  });
});

describe('TrafficControl', () => {
  it('should be a TrafficControl', () => {
    expect(TrafficControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(TrafficControl)))"`
    );
  });
});

describe('TypeSelector', () => {
  it('should be a TypeSelector', () => {
    expect(TypeSelector.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(TypeSelector)))"`
    );
  });
});

describe('ZoomControl', () => {
  it('should be a ZoomControl', () => {
    expect(ZoomControl.displayName).toMatchInlineSnapshot(
      `"withParentContext(withYMapsContext(withYMaps(ZoomControl)))"`
    );
  });
});
