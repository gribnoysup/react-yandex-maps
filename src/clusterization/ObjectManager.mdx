---
name: ObjectManager
route: /clusterization/ObjectManager
menu: Clusterization
---

import { Playground } from 'docz';
import { Props } from '../../docz/components/Props';
import { YMaps, Map, ObjectManager } from '../';
import { ObjectManager as _ObjectManager } from './ObjectManager';
import features from './__fixtures__/features';

# ObjectManager

Renders Yandex.Maps API [ObjectManager][docs]

## Basic Usage

<Playground>
  <YMaps>
    <Map
      defaultState={{
        center: [55.751574, 37.573856],
        zoom: 10,
      }}
    >
      <ObjectManager
        options={{
          clusterize: true,
          gridSize: 32,
        }}
        objects={{
          openBalloonOnClick: true,
          preset: 'islands#greenDotIcon',
        }}
        clusters={{
          preset: 'islands#redClusterIcons',
        }}
        filter={object => object.id % 2 === 0}
        defaultFeatures={features}
        modules={[
          'objectManager.addon.objectsBalloon',
          'objectManager.addon.objectsHint',
        ]}
      />
    </Map>
  </YMaps>
</Playground>

## Props

<Props of={_ObjectManager} />

[docs]:
  https://tech.yandex.com/maps/doc/jsapi/2.1/ref/reference/control.ObjectManager-docpage/
